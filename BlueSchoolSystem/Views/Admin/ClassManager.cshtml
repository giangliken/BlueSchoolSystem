@model IEnumerable<BlueSchoolSystem.Models.ViewModel.LopHocViewModel>

@{
    ViewData["Title"] = "ClassManager";
}

<h1>Quản lý lớp học</h1>

<div class="mb-3"><b>Tổng số:</b> @Model.Count() bản ghi</div>
<table class="table table-hover table-striped table-bordered">
    <thead class="table-dark">
        <tr>
            <th class="text-center">STT</th>
            <th class="text-center">Mã lớp</th>
            <th class="text-center">Tên lớp</th>
            <th class="text-center">Ngành học</th>
            <th class="text-center">Trực thuộc</th>
            <th class="text-center">Sỉ số hiện tại</th>
            @* <th class="text-center">Mô tả</th> *@
        </tr>
    </thead>
    <tbody>
        @{
            foreach (var item in Model)
            {
                <tr style="cursor:pointer"
                    onclick="window.location='@Url.Action("ClassDetails", "Admin", new { id = item.Id })'">
                    <td style="width: 60px;" class="text-center">@item.Id</td>
                    <td style="width: 60px;" class="text-start">@item.MaLop</td>
                    <td style="width: 150x;" class="text-start">@item.TenLop</td>
                    <td style="width: 150x;" class="text-start">@item.Nganh</td>
                    <td style="width: 150x;" class="text-start">@item.Khoa</td>
                    <td style="width: 150px;" class="text-center">@item.SiSo</td>
                </tr>
            }
        }
    </tbody>
</table>
<p style="color:black"><i>*Nếu thông tin lớp học có sai sót thì liên hệ quản trị viên hệ thống</i></p>
@section Scripts {
    
}
